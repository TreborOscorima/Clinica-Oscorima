# Dependencias directas (fuente editable). Los pines exactos viven en
# requirements.lock (prod) y requirements-dev.lock (CI), generados con
# pip-compile DENTRO de python:3.13-slim para que resuelvan igual que prod.
# Regenerar tras cambiar algo aquí:
#   pip-compile --strip-extras --output-file=requirements.lock requirements.txt
#   pip-compile --strip-extras --output-file=requirements-dev.lock requirements.txt requirements-dev.txt
#
# ─── Framework Full-Stack ─────────────────────────────────────────────────────
reflex==0.9.8

# ─── Base de Datos ────────────────────────────────────────────────────────────
sqlmodel>=0.0.22
SQLAlchemy>=2.0.31
PyMySQL>=1.1.1
aiomysql>=0.3.0
greenlet>=3.0

# ─── Autenticación ────────────────────────────────────────────────────────────
bcrypt>=4.2.1

# ─── Exportación ──────────────────────────────────────────────────────────────
openpyxl>=3.1.2
reportlab>=4.0.0

# ─── Notificaciones (email nativo smtplib; WhatsApp vía Twilio) ───────────────
twilio>=7.0.0

# ─── Observabilidad de errores (no-op sin SENTRY_DSN) ─────────────────────────
sentry-sdk>=2.0.0

# ─── Núcleo compartido TUWAYKI (mismo patrón que TUWAYKIFOOD) ─────────────────
tuwayki-core @ git+https://github.com/TreborOscorima/tuwayki-core.git@ef852f2

# ─── Config ───────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
cryptography>=44.0.3

# ─── Zonas horarias (scheduler de recordatorios) ──────────────────────────────
# La imagen slim no trae la base del sistema; sin esto zoneinfo no resuelve
# nombres como "America/Lima" y el scheduler caería a UTC (hora equivocada).
tzdata>=2025.1

# ─── Migraciones ──────────────────────────────────────────────────────────────
alembic>=1.13.0
