# Dependencias de desarrollo / CI (no van a producción) — fuente editable.
# El CI instala los pines exactos desde requirements-dev.lock (prod + dev).
# Uso local rápido: pip install -r requirements.txt -r requirements-dev.txt
# Regenerar el lock tras cambios (ver cabecera de requirements.txt).
#
# ─── Tests ────────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20          # backend SQLite async para la suite (no toca MySQL)

# ─── Linter / formateador ─────────────────────────────────────────────────────
ruff>=0.16
